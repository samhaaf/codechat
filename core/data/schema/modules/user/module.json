{
    "name": "postgres",
    "depends_on": ["postgres"],
    "applies_to": "database",
    "database": {
        "$ref": "./database.json"
    },
    "variables": {
        "database_name": {
            "type": "string",
            "default": "postgres"
        }
    }
}
